export const TARGET_TEXT =
  "system or hand make then see stand you so stand follow those again";

export const MAX_TYPED_LENGTH = TARGET_TEXT.length;

export const RECENT_SPEED_WINDOW_MS = 1200;

export const ROCKET_TRAVEL_PERCENT = 72;

export const MAX_ALTITUDE_KM = 15000;

export const MIN_ELAPSED_SECONDS_FOR_WPM = 1;
